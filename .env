# Rename this file to `.env` and fill in the appropriate values for your deployment.

# The port this gateway will listen on for incoming SIP (UDP/TCP) traffic.
PORT=5060

# Incoming (Meta/WhatsApp) SIP credentials.
# These values are provided by the WhatsApp Business Calling API.  The username
# should be the phone_number_id and the password is returned when
# calling the settings endpoint with `include_sip_credentials=true`.
META_SIP_USERNAME=599170193276193
META_SIP_PASSWORD=wTgO4RZS0mqq23mQVrZhVyn1xVK9NBms

# Outgoing (Twilio) SIP credentials.
# These correspond to a Twilio Credential List associated with your SIP Domain.
TWILIO_SIP_USERNAME=nonocard
TWILIO_SIP_PASSWORD=$EbxHNCtL2oHuSY%^Y62*

# The Twilio SIP Domain that will receive calls forwarded from this gateway.
TWILIO_SIP_DOMAIN=nonocard.sip.twilio.com

# The destination user part to call when forwarding the call to Twilio.
# Usually this is the WhatsApp number in E.164 format without the plus sign.
TWILIO_DESTINATION=+5215541655565

# Webhook URL used by Twilio to control the call once it is accepted.
# Twilio will request this URL for TwiML instructions when the SIP Domain receives a call.
WEBHOOK_URL=https://mmh.app.n8n.cloud/webhook/twiml5

# Optional TLS settings.  If USE_TLS=true then TLS transport will be enabled and
# TLS_CERT_PATH and TLS_KEY_PATH must point to certificate and private key files.
USE_TLS=false
TLS_CERT_PATH=cert.pem
TLS_KEY_PATH=privkey.pem