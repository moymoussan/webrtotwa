{
  "name": "whatsapp-to-twilio-gateway",
  "version": "1.0.0",
  "description": "SIP gateway that receives WhatsApp Business Calling SIP invites and forwards them to a Twilio SIP Domain while rewriting the SDP and authenticating as required.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "sip": "^0.13.0",
    
    "sdp-transform": "^2.17.2",
    "dotenv": "^16.3.1"
  }
}